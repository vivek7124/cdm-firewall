info:
  title: Allow THD Cassandra OpsCenter Rule
  author: John McKenna
  description: Creates a firewall rule to allows THD IP space to reach Cassandra's OpsCenter.
  version: 1.0

imports:
- name: firewall.jinja
  path: ../cdm-templates/resources/firewall.jinja

required:
- network

network:
  type: string
  default: internal
  description: The network in the Google Cloud project that rule will apply to.
